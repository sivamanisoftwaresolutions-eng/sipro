rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write during prototyping / enterprise operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
